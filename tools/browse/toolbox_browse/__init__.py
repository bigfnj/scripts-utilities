"""browse - escalating web fetch for agent-driven research.

Kept in step with pyproject.toml by hand; tests/Invoke-InstallerTests.ps1
asserts the two agree, because smoke-test.ps1 compares an INSTALLED browse
against this number to catch "edited the repo, never reinstalled" - and if the
two copies disagree with each other, that check measures the wrong thing.
"""

__version__ = "0.1.0"
